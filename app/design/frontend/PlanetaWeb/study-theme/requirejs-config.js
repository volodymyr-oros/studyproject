var config = {
    map: {
        '*': {
            slickCarousel: 'js/slick-carousel',
        }
    },
    paths: {
        slick: 'js/slick.min'
    },
    shim: {
        slick: {
            deps: ['jquery']
        }
   }
};
